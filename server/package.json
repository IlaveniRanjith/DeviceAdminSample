{
  "name": "device-admin-remote-server-root",
  "version": "1.0.0",
  "description": "Unified starter for Device Admin backend and frontend",
  "scripts": {
    "setup": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "start": "concurrently \"npm start --prefix backend\" \"npm start --prefix frontend\""
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}